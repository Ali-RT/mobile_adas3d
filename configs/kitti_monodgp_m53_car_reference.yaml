base_config: kitti_mnv4_axis_direction_v5.yaml

# Official Car-only taxonomy for reproducing the pinned MonoDGP checkpoint.
# This is intentionally not the MobileADAS3D Vehicle/Pedestrian product taxonomy.
dataset:
  classes: [Car]
  class_mapping: null
  require_taxonomy_manifest: false
